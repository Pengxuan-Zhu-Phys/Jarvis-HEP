# D7.1 acceptance benchmarks

Machine-relative gate results for WP-D7.1 (`tests/test_distributed_acceptance.py`).

- `d7_1_acceptance.json` — latest recorded gate metrics (regenerate by running the acceptance suite).
- Numbers are host-specific; gates scale thresholds to `os.cpu_count()` where noted in the plan.