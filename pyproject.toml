[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Jarvis-HEP"
version = "2.0.1"
description = "Jarvis-HEP V2 distributed runtime (Redis + spawn Workers)"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "Jarvis-HEP Developers" },
]
dependencies = [
    "h5py>=3.8",
    "jsonschema>=4.18",
    "numpy>=1.24",
    "scipy>=1.10",
    "sympy>=1.12",
    "PyYAML>=6.0",
    "setproctitle>=1.3.3",
    "Jarvis-HEP-Portal>=1.5.1",
    "Jarvis-Operas>=1.4.1",
    "Jarvis-PLOT>=1.3.3",
    "typer>=0.25.0",
]

[project.scripts]
Jarvis = "jarvishep2.client:main"

[project.optional-dependencies]
distributed = [
    "redis>=5.0",
    "msgpack>=1.0",
    "aiofiles>=23.0",
]
# Deprecated alias: Operas is a core dependency since D12.0. Kept so existing
# `pip install '.[operas]'` scripts remain valid.
operas = [
    "Jarvis-Operas>=1.4.1",
]
plot = [
    "Jarvis-PLOT>=1.3.3",
]
dev = [
    "pytest>=7.0",
    "fakeredis>=2.0",
    "colorlog>=6.0",
]

[project.urls]
Homepage = "https://github.com/Pengxuan-Zhu-Phys/Jarvis-HEP"
Documentation = "https://pengxuan-zhu-phys.github.io/Jarvis-Docs/"
Repository = "https://github.com/Pengxuan-Zhu-Phys/Jarvis-HEP"
Issues = "https://github.com/Pengxuan-Zhu-Phys/Jarvis-HEP/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["jarvishep2*"]

[tool.setuptools.package-data]
jarvishep2 = [
    "card/*",
    "card/**/*",
    "schema/*.json",
    "schema/**/*.json",
    "project_template/*",
    "project_template/**/*",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "--ignore=tests/test_adaptive_bridson.py",
    "--ignore=tests/test_ensemble_samplers.py",
    "--ignore=tests/test_distributed_acceptance.py",
    "--ignore=tests/test_distributed_resume.py",
    "--ignore=tests/test_mcmc_sampler.py",
    "--ignore=tests/test_worker_pool.py",
    "--ignore=tests/test_variable_distributions.py",
    "--ignore=tests/test_worker_failure.py",
]
