# D18.5 verified LibDeps skill card: the library command is deliberately harmless.
Scan:
  name: shared-libraries-skill
Sampling:
  Method: Random
  Bounds:
    point_number: 1
  Variables:
    - name: x
      distribution:
        type: Flat
        parameters:
          min: 0.0
          max: 1.0
LibDeps:
  path: "&J/deps/library"
  make_parallel: 4
  Modules:
    - name: ExampleSharedLibrary
      required_modules: []
      installed: false
      installation:
        path: "${LibDeps:path}/ExampleSharedLibrary"
        commands:
          - "mkdir -p ${path}"
          - "true"

EnvReqs: {}
Calculators: {}
